# PostgreSQL Configuration for Patroni HA Cluster
# Optimized for Zipline with automatic failover
# ==============================================

# Connection settings
listen_addresses = '*'
port = 5432
max_connections = 100

# Memory settings
shared_buffers = 256MB
effective_cache_size = 1GB
work_mem = 4MB
maintenance_work_mem = 64MB

# WAL settings for replication (Patroni managed)
wal_level = replica
wal_keep_size = 128MB
max_wal_senders = 10
max_replication_slots = 10
wal_sender_timeout = 60s

# Checkpointing
checkpoint_completion_target = 0.9
wal_buffers = 16MB
checkpoint_timeout = 5min
max_wal_size = 1GB
min_wal_size = 80MB

# Hot standby settings
hot_standby = on
hot_standby_feedback = on
wal_receiver_timeout = 60s
max_standby_streaming_delay = 30s

# Logging
logging_collector = on
log_destination = 'stderr'
log_directory = 'log'
log_filename = 'postgresql-%Y-%m-%d_%H%M%S.log'
log_rotation_age = 1d
log_rotation_size = 100MB
log_min_messages = warning
log_error_verbosity = default
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '
log_checkpoints = on
log_connections = on
log_disconnections = on
log_lock_waits = on
log_temp_files = 10MB
log_autovacuum_min_duration = 0
log_statement = 'ddl'

# Performance monitoring
track_activities = on
track_counts = on
track_functions = all
track_io_timing = on

# Background writer
bgwriter_delay = 200ms
bgwriter_lru_maxpages = 100
bgwriter_lru_multiplier = 2.0

# Security
ssl = off # Enable in production with proper certificates
password_encryption = scram-sha-256

# Autovacuum
autovacuum = on
autovacuum_max_workers = 3
autovacuum_naptime = 1min
autovacuum_vacuum_threshold = 50
autovacuum_vacuum_scale_factor = 0.2
autovacuum_analyze_threshold = 50
autovacuum_analyze_scale_factor = 0.1

# Patroni specific settings (will be managed by Patroni)
# synchronous_standby_names = ''  # Managed by Patroni
# archive_mode = on  # Managed by Patroni
# archive_command = '/bin/true'  # Managed by Patroni
